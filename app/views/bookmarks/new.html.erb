  <%= simple_form_for [@list, @bookmark], defaults: { input_html: { class: 'form-group' } } do |f| %>
    <%= f.association :movie%>
    <%= f.input :comment %>
    <%= f.button :submit, class: 'btn btn-primary'  %>
  <% end %>
